model{

  # Adult numbers


  for(i in 1:years) {

  Nadult[i] ~ dunif(0, 10000) }

	
  for(j in 1:nobs) {
    
    TruePairs[j] ~ dbinom(
      (RObase[j]) /
      (Nadult[year[j]]),
      Pair_viable_count_per_agedif[j]
    )
  }}