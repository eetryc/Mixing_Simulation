This repo has all the iterations of the stock mixing population simulation.

0.example holds the initial run mimicking another modeler's code to adjust to syntax (this file is largely irrelevant now).

1.simple holds the initial runs of a simulation and a CKMR model in their simplest forms.
